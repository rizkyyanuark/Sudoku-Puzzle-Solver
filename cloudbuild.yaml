steps:
  # Build Docker image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "asia-southeast2-docker.pkg.dev/${PROJECT_ID}/sudoku-solver/sudoku-solver-service", "."]
    env:
      - "DOCKER_BUILDKIT=1"

  # Push Docker image
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "asia-southeast2-docker.pkg.dev/${PROJECT_ID}/sudoku-solver/sudoku-solver-service"]

options:
  logging: CLOUD_LOGGING_ONLY
